# **🧠 인공지능의 학습 비결: 역전파(Backpropagation) 아주 쉽게 이해하기**

인공지능이 정답을 맞히기 위해 스스로 공부하는 과정을 '학습'이라고 합니다. 이때 핵심이 되는 기술이 바로 **역전파**입니다. 어려운 수식 대신 원리 중심으로 정리해 보았습니다.

## **1\. 역전파의 목적: "실수를 줄이자\!"**

인공지능이 예측한 값과 실제 정답 사이에는 차이(오차)가 생깁니다. 이 오차를 손실(Loss) 혹은 \*\*비용(Cost)\*\*이라고 부릅니다.  
역전파의 목표는 \*\*"가중치(Weight)를 어떻게 바꿔야 이 손실을 줄일 수 있을까?"\*\*를 알아내는 것입니다.

## **2\. 경사하강법: 산을 내려가는 가장 빠른 길**

손실을 줄이는 방법은 마치 안개가 자욱한 산에서 골짜기(최저점)를 찾아 내려가는 것과 같습니다.

* **원리:** 지금 서 있는 곳의 기울기를 보고, 아래쪽 방향으로 조금씩 이동합니다.  
* **공식의 의미:** 새로운 가중치 \= 현재 가중치 \- (기울기 × 학습률)  
  * 기울기가 \*\*양수(+)\*\*면: 왼쪽(작은 방향)으로 이동합니다.  
  * 기울기가 \*\*음수(-)\*\*면: 오른쪽(큰 방향)으로 이동합니다.  
  * **학습률:** 한 번에 얼마나 멀리 이동할지 결정하는 보폭입니다.

## **3\. 편미분: "범인 찾기"**

신경망에는 수많은 가중치가 있습니다. 전체 오차($C$)에 대해 특정 가중치($W\_5$)가 얼마나 잘못했는지를 알아내야 합니다.

* **편미분이란?** 다른 가중치들은 가만히 두고, 내가 궁금한 **단 하나의 가중치만 살짝 변했을 때** 결과가 얼마나 변하는지 확인하는 것입니다.  
* **예시:** 요리 맛이 없을 때, 다른 재료는 그대로 두고 소금($W\_5$)만 조금 더 넣었을 때 맛($C$)이 어떻게 변하는지 보는 것과 같습니다.

## **4\. \[상세 해설\] 계산 과정 따라가기 (Step-by-Step)**

PDF에 나온 $W\_5$를 업데이트하기 위해, 우리는 **"가중치** $W\_5$**가 손실** $C$**에 얼마나 영향을 주는가?"** 즉, $\\frac{\\partial C}{\\partial w\_5}$를 구해야 합니다. 이를 위해 \*\*체인룰(연쇄 법칙)\*\*을 3단계로 쪼개서 계산합니다.

### **1단계: 출력값($O\_1$)이 변할 때 오차($C$)가 얼마나 변하는가? ($\\frac{\\partial C}{\\partial o\_1}$)**

가장 마지막 단계입니다. 내가 내놓은 답($O\_1$)과 실제 정답 사이의 거리를 미분합니다.

* **직관적 의미:** "내 대답이 조금 바뀌면 오차가 얼마나 줄어들까?"를 계산합니다.

### **2단계: 신호 합계($Z\_3$)가 변할 때 출력값($O\_1$)이 얼마나 변하는가? ($\\frac{\\partial o\_1}{\\partial z\_3}$)**

보통 활성화 함수(Sigmoid 등)를 통과하는 단계입니다.

* **직관적 의미:** "활성화 함수에 들어가는 입력 신호가 살짝 변할 때, 최종 출력값은 얼마나 민감하게 반응할까?"를 계산합니다.

### **3단계: 가중치($W\_5$)가 변할 때 신호 합계($Z\_3$)가 얼마나 변하는가? ($\\frac{\\partial z\_3}{\\partial w\_5}$)**

가장 앞부분의 연결 통로입니다. $Z\_3 \= W\_5 \\times h\_1 \+ \\dots$ 이므로, $W\_5$ 앞에 곱해진 이전 층의 신호($h\_1$) 값이 곧 기울기가 됩니다.

* **직관적 의미:** "$W\_5$라는 연결 고리의 강도를 조절하면, 다음으로 전달되는 신호 합계가 얼마나 변할까?"를 계산합니다.

### **최종 결합: 모두 곱하기**

$$\\frac{\\partial C}{\\partial w\_5} \= \\frac{\\partial C}{\\partial o\_1} \\times \\frac{\\partial o\_1}{\\partial z\_3} \\times \\frac{\\partial z\_3}{\\partial w\_5}$$

이 세 값을 모두 곱하면, 비로소 \*\*"$W\_5$를 이만큼 바꿨을 때 최종 오차가 이만큼 변한다"\*\*는 전체 그림이 완성됩니다.

## **5\. 테일러 근사: "살짝의 변화 관찰"**

수학적으로 복잡한 곡선도 아주 좁은 구간에서 보면 직선처럼 보입니다.

* "손실함수의 변화량($\\Delta C$)은 기울기($\\nabla C$)에 가중치의 변화량($\\Delta W$)을 곱한 것과 거의 같다"는 원리입니다.  
* 이를 통해 가중치를 아주 조금만 바꿔도 손실값이 어떻게 변할지 미리 예측할 수 있습니다.

## **📝 요약하자면?**

1. **결과 확인:** 정답과 얼마나 다른지 확인합니다. (현재 오차 $C=0.126$)  
2. **거꾸로 추적(역전파):** 출력층부터 입력층 방향으로 단계별 기울기를 곱해 나갑니다.  
3. **수정:** 계산된 최종 기울기($\\frac{\\partial C}{\\partial w\_5}$)를 이용해 경사하강법으로 가중치를 업데이트합니다.  
4. **반복:** 이 과정을 수만 번 반복하면 오차 $C$가 점점 $0$에 가까워지며 인공지능이 똑똑해집니다\!