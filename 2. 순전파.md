# **🤖 AI는 어떻게 답을 낼까요? (1단계: 순전파)**

인공신경망이 데이터를 입력받아 최종 예측값을 내놓는 과정을 \*\*순전파(Feedforward)\*\*라고 합니다. 이름 그대로 정보가 앞에서 뒤로(입력 → 출력) 흐르기 때문에 붙여진 이름입니다.

## **1\. 준비물: AI의 기본 구성**

AI가 학습을 시작하기 전, 다음과 같은 기본 설정이 필요합니다.

* **입력값 (**$x$**):** AI에게 주는 힌트입니다. (예: $0.5, 0.3$)  
* **가중치 (**$w$**):** 각 힌트가 얼마나 중요한지 나타내는 '중요도'입니다. 처음에는 임의의 값을 가집니다.  
* **활성화 함수 (시그모이드):** 계산된 결과를 $0$과 $1$ 사이의 값으로 변환해주는 '필터'입니다.  
* **정답 (Target):** AI가 맞혀야 할 실제 목표값입니다. (예: $0.8$)

## **2\. \[1단계\] 입력받고 중요도 곱하기**

입력받은 값에 각각의 중요도(가중치)를 곱해서 더합니다. 이것을 **가중합**이라고 합니다.

### **1층에서 일어나는 일 (은닉층 계산)**

* **첫 번째 노드 (**$z\_1$**):** $(0.5 \\times 0.7) \+ (0.3 \\times 0.4) \= 0.47$  
* **두 번째 노드 (**$z\_2$**):** $(0.5 \\times 0.3) \+ (0.3 \\times 0.6) \= 0.33$

💡 **쉽게 말하면?** "데이터 $0.5$는 $70%$만큼 중요하고, 데이터 $0.3$은 $40%$만큼 중요해\!"라고 판단하는 과정입니다.

## **3\. \[2단계\] 필터 통과시키기 (활성화 함수)**

계산된 값($0.47, 0.33$)을 그대로 쓰지 않고, \*\*시그모이드($\\sigma$)\*\*라는 필터에 통과시킵니다. 이 필터는 어떤 숫자든 $0$에서 $1$ 사이의 부드러운 값으로 바꿔줍니다.

* **결과 1 (**$h\_1$**):** $\\sigma(0.47) \\approx 0.615$  
* **결과 2 (**$h\_2$**):** $\\sigma(0.33) \\approx 0.582$

💡 **왜 하나요?** 계산 값이 너무 커지거나 작아지는 것을 막고, 복잡한 패턴을 학습할 수 있게 도와줍니다.

## **4\. \[3단계\] 최종 예측 결과 내기**

이제 은닉층에서 나온 결과들을 다시 한번 중요도($w\_5, w\_6$)와 곱해서 최종 결과값을 만듭니다.

1. **다시 곱하고 더하기 (**$z\_3$**):** $(0.615 \\times 0.55) \+ (0.582 \\times 0.45) \\approx 0.6$  
2. **마지막 필터 통과 (**$o\_1$**):** $\\sigma(0.6) \\approx \\mathbf{0.645}$

**🎉 드디어 AI가 예측을 마쳤습니다\! AI의 대답은** $0.645$**입니다.**

## **5\. \[4단계\] 얼마나 틀렸을까? (오차 계산)**

AI의 예측값($0.645$)과 실제 정답($0.8$)을 비교해 봅니다. 이 차이를 \*\*손실(Loss)\*\*이라고 부릅니다.

* **오차 (**$E$**):** $\\frac{1}{2} \\times (0.8 \- 0.645)^2 \\approx \\mathbf{0.012}$

💡 **결론:** AI는 지금 "정답보다 조금 낮게 대답했네?"라고 깨닫는 중입니다. 이 $0.012$라는 오차를 줄이기 위해 다음 단계에서 중요도(가중치)를 스스로 수정하게 됩니다.

### **🌟 핵심 포인트 요약**

1. **순전파:** 데이터를 넣고 계산해서 결과를 내는 과정.  
2. **가중치 (**$w$**):** AI가 판단하는 데이터의 '중요도'.  
3. **활성화 함수:** 결과를 적절한 범위($0 \\sim 1$)로 조절하는 장치.  
4. **오차:** 정답과 얼마나 다른지 확인하는 척도.